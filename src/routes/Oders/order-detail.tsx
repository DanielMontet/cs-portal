import { AppProps } from "../../types/props.types";

const OrderDetail: React.FunctionComponent<AppProps> = (props) => {
  return <section className="bg-white rounded-lg shadow h-full"></section>;
};

export default OrderDetail;
